# Skland Account Skill

Lightweight Skland account skill.

Current features:

- Manual bind with `token` or `cred`
- Refresh and store bound Arknights roles
- Query a text summary for the default role
- Unbind and clear local stored credentials

The first version intentionally skips QR login, sign-in, and gacha history.

